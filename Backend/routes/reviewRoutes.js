const express = require('express');
const router = express.Router();

// Example: Get all reviews (placeholder)
// router.get('/', (req, res) => {
//   res.json({ message: 'List of reviews' });
// });

module.exports = router; 